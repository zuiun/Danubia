Document code (doxygen?)

Everything that must not fail -> main.cpp; everything that can otherwise be replaced -> game.cpp
Create scene loading scheme
Create characters
Create maps and movement
Create modifiers
Create tile effects
Create modifier logic system
Create skills
Add dialogue

UI should be a tree, with lowest (layer) elements at root and highest (layer) elements at leaves, and should be rendered pre-order
